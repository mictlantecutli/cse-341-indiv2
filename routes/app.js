//const routesDoc = require('express').Router();
//const swaggerUi = require('swagger-ui-express');
//const swaggerDocument = require('../swagger-output.json');

//routesDoc.use('/api-docs', swaggerUi.serve);
//routesDoc.get('/api-docs', swaggerUi.setup(swaggerDocument));

//module.exports = routesDoc